import React, {Component} from 'react';

class App extends Component {
  render() {
    return (
    <div>
      <button onClick = {this.handleClick}>Calculate my Score</button>
    </div>
    );
  }
}
